{
	"name": "@cxl/rx",
	"description": "Lightweight reactiveX implementation",
	"version": "0.1.0",
	"license": "Apache-2.0",
	"files": [
		"index.js",
		"index.d.ts"
	],
	"scripts": {
		"build": "node build",
		"test": "node build && cd ../dist/rx && node ../tester",
		"docs": "rm -rf ../docs/rx && node ../dist/docgen -o ../docs/rx --repository \"https://github.com/cxlio/cxl/tree/`git rev-parse --abbrev-ref HEAD`/rx\"",
		"package": "npm test && node build package",
		"prepublishOnly": "npm run package",
		"publishDocs": "npm run docs",
		"publish": "cd ../dist/rx && npm publish && npm run publishDocs"
	},
	"homepage": "https://coaxialhost.com/rx",
	"repository": "https://github.com/cxlio/cxl/tree/$BRANCH/rx"
}
