{
	"name": "@cxl/dom",
	"version": "0.0.1",
	"description": "Reactive DOM wrapper",
	"license": "GPL-3.0",
	"scripts": {
		"build": "node ../dist/build",
		"test": "node ../dist/build && cd ../dist/dom && node ../tester"
	}
}
