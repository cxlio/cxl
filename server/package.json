{
	"name": "@cxl/server",
	"version": "0.0.1",
	"description": "Application library",
	"license": "GPL-3.0",
	"scripts": {
		"build": "node ../dist/build",
		"test": "node ../dist/build && cd ../dist/server && node ../tester --node"
	}
}
