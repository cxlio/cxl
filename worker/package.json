{
	"name": "@cxl/worker",
	"version": "0.0.1",
	"description": "Web Worker library",
	"license": "GPL-3.0",
	"scripts": {
		"build": "node ../dist/build",
		"test": "node ../dist/build && cd ../dist/worker && node ../tester"
	}
}
