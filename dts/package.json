{
	"name": "@cxl/dts",
	"description": "Generate symbol information from typescript source code",
	"license": "GPL-3.0-only",
	"version": "0.0.1",
	"scripts": {
		"build": "node ../dist/build",
		"test": "node ../dist/build && cd ../dist/dts && node ../tester --node"
	}
}
