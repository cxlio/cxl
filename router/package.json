{
	"name": "@cxl/router",
	"version": "0.1.0",
	"files": [
		"index.js",
		"index.d.ts"
	],
	"scripts": {
		"build": "node build",
		"test": "npm run build && cd ../dist/router && node ../tester/runner"
	},
	"devDependencies": {
		"typescript": "^3.6.2"
	}
}
