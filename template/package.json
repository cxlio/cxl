{
	"name": "@cxl/template",
	"version": "1.0.0",
	"description": "Reactive template library",
	"license": "GPL-3.0",
	"scripts": {
		"build": "node ../dist/build",
		"test": "node ../dist/build && cd ../dist/template && node ../tester"
	}
}
